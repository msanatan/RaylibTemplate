{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "raygame (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb"
        }
    ]
}
